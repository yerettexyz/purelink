# 🚫 Unsupported Domains (403/Blocked)

The following domains are currently known to block automated resolution (Cloudflare 403 or server-side blocks). Purelink will still attempt to scrub tracking parameters from these links, but will not attempt to unwrap/resolve the redirect chain.

### Known Blocked Domains:
- `mavelylife.com`
- `mavelyinfluencer.com`
- `inflcm.com`
- `theblogcm.com`
- `dealshacks.com`
- `walmrt.us`
- `walmart.com`
